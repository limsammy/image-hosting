# =============================================================================
# DATABASE
# =============================================================================
# Local development: Use imagehosting_dev (created by scripts/setup-local-db.sh)
# Production: Use your production database name
# Docker: Uses internal networking with POSTGRES_HOST=db (set in docker-compose.yml)

POSTGRES_USER=imagehosting_user
POSTGRES_PASSWORD=imagehosting_dev_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=imagehosting_dev
POSTGRES_DB_DEV=imagehosting_dev

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
JWT_SECRET_KEY=generate-a-32-char-minimum-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=10080

# =============================================================================
# CLOUDFLARE R2 STORAGE
# =============================================================================
# See docs/setup.md for R2 configuration instructions
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY=your_r2_access_key
R2_SECRET_KEY=your_r2_secret_key
R2_BUCKET_NAME=image-hosting
R2_PUBLIC_URL=https://pub-xxx.r2.dev
# Optional: R2 API token (for reference only, not used by app)
# R2_TOKEN=your_r2_token
# Optional: Custom S3 endpoint (usually not needed)
# R2_JURISDICTION_URL=https://your_account_id.r2.cloudflarestorage.com

# =============================================================================
# APPLICATION
# =============================================================================
LOG_LEVEL=DEBUG
FRONTEND_URL=http://localhost:5173
